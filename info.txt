1) indexOf() in JS gives a number (index or -1) whereas findIndex() returns the index too but accepts a function 
    as an argument instead of a value like indexOf.
    - MDN : The findIndex() method returns the index of the first element in the array that satisfies the 
        provided testing function. Otherwise, it returns -1, indicating that no element passed the test.

2) find() in JS accpets a function and returns the first value found paasing the condition in the function.